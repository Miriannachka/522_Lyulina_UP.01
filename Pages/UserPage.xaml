<Page x:Class="_522_Lyulina.Pages.UserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_522_Lyulina.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Список пользователей">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">

            <TextBlock Text="Введите ФИО для поиска:" FontSize="12"
                       VerticalAlignment="Center" Margin="10,0,0,0"/>

            <TextBox x:Name="fioFilterTextBox" Margin="7" MinWidth="120" Height="30"
                     TextChanged="fioFilterTextBox_TextChanged"/>

            <TextBlock Text="Сортировка по ФИО:" Margin="10,0,0,0" FontSize="12"
                       VerticalAlignment="Center"/>

            <ComboBox SelectionChanged="sortComboBox_SelectionChanged"
                      Margin="7" x:Name="sortComboBox" SelectedIndex="0"
                      VerticalContentAlignment="Center" VerticalAlignment="Center" Width="120" Height="30">
                <ComboBoxItem Content="По возрастанию"/>
                <ComboBoxItem Content="По убыванию"/>
            </ComboBox>

            <CheckBox IsChecked="False" Checked="onlyAdminCheckBox_Checked"
                      Unchecked="onlyAdminCheckBox_Unchecked" Content="Только администраторы" 
                      x:Name="onlyAdminCheckBox"
                      VerticalContentAlignment="Center" Margin="10,0,0,0" Height="30"/>

            <Button x:Name="clearFiltersButton" Content="Очистить фильтр"
                    VerticalAlignment="Center" HorizontalAlignment="Center" 
                    Padding="10,5" Margin="10"
                    Height="30" FontSize="12" Click="clearFiltersButton_Click_1"/>
        </WrapPanel>

        <ListView Name="ListUser" Grid.Row="1"
                  HorizontalContentAlignment="Center"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Margin="10">

            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"
                               HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Width="220" Margin="10" Background="WhiteSmoke">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="200"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock VerticalAlignment="Center"
                                   TextAlignment="Center" Width="200" Grid.Row="0" TextWrapping="Wrap"
                                   Text="{Binding FIO}" HorizontalAlignment="Center" 
                                   FontSize="14" FontWeight="Bold" Margin="5"/>

                        <Image Width="200" Grid.Row="1" Stretch="UniformToFill"
                               HorizontalAlignment="Center">
                            <Image.Source>
                                <Binding Path="Photo">
                                    <Binding.TargetNullValue>
                                        <ImageSource>images/default.png</ImageSource>
                                    </Binding.TargetNullValue>
                                </Binding>
                            </Image.Source>
                        </Image>

                        <TextBlock VerticalAlignment="Center" TextAlignment="Center"
                                   Width="200" Grid.Row="2" HorizontalAlignment="Center" 
                                   FontSize="14" Margin="5"
                                   Text="{Binding Role}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>