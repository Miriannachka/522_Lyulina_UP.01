<Window x:Class="_522_Lyulina.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_522_Lyulina"
        mc:Ignorable="d"
        Title="522_Lyulina" Height="450" Width="800"
        Loaded="Window_Loaded"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>

        <Button Content="Назад"
                Grid.Row="0"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Margin="10,0,0,0"
                Width="100"
                Height="30"
                x:Name="BackButton"
                Click="Back_Click"
                />

        <ComboBox x:Name="StyleComboBox"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Width="150"
                  Height="30"
                  Margin="0,0,10,0"
                  SelectedIndex="0"
                  SelectionChanged="StyleComboBox_SelectionChanged">
            <ComboBoxItem Content="Оригинальная тема"/>
            <ComboBoxItem Content="Темно-зеленая тема"/>
        </ComboBox>

        <Frame x:Name="MainFrame"
               Grid.Row="1"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch"
               NavigationUIVisibility="Hidden" />
        <TextBlock x:Name="DateTimeNow"
                   Grid.Row="2"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Margin="0,0,10,0"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Текущая дата и время" />

    </Grid>
</Window>
